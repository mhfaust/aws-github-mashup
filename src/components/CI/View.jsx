import React from 'react';
import { Table, Icon, Dropdown, Loader, Dimmer } from 'semantic-ui-react';
import { StickyContainer, Sticky } from 'react-sticky';

const View = ({ builds }) => { return (
    <main className="CI">
      <h1>Continuous Integration</h1>
      
    </main>
  );
};

export default View;
