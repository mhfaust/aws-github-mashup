import React from 'react';
import { Table, Icon, Dropdown, Loader, Dimmer } from 'semantic-ui-react';
import { StickyContainer, Sticky } from 'react-sticky';

const View =
() => { return (
  <main className="config">
    <h1>Connect Dashboard Configuration</h1>
    <p>(not implemented)</p>
  </main>
  );
}

export default View;
